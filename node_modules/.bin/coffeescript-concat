#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../coffeescript-concat/coffeescript-concat" "$@"
  ret=$?
else 
  node  "$basedir/../coffeescript-concat/coffeescript-concat" "$@"
  ret=$?
fi
exit $ret
